class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        # right = len(nums)-1

        # dic = {}

        # for i, num in enumerate(nums):
        #     dict[i] = num

        # TO-DO